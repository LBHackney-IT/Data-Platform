module "load_locations_vaccine_to_refined_data_and_insight" {
  source = "../modules/aws-glue-job"

  department  = module.department_data_and_insight
  job_name    = "${local.short_identifier_prefix}covid_locations_and_vaccinations"
  script_name = "covid_locations_and_vaccinations"
  job_parameters = {
    "--cleaned_covid_locations_s3_bucket_target" = "${module.refined_zone.bucket_id}/data-and-insight/covid-locations-vaccinations-cleaned"
    "--source_catalog_database" = "data-and-insight-raw-zone"
    "--source_catalog_table" = "data_and_insight_covid_locations"
    "--source_catalog_table2" = "data_and_insight_covid_vaccinations"
  }
  crawler_details = {
    database_name      = module.department_data_and_insight.refined_zone_catalog_database_name
    s3_target_location = "${module.refined_zone.bucket_id}/data-and-insight/covid-locations-vaccinations-cleaned"
  }
}
